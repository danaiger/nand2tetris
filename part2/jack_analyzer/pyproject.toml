[project]
name = "jack-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dan Naiger", email = "dannitzhak@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
jack-analyzer = "jack_analyzer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
